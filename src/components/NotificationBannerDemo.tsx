import React from 'react';
import NotificationBanner from './NotificationBanner';

const NotificationBannerDemo: React.FC = () => {
  return (
    <div style={{ width: '100%', maxWidth: '800px', margin: '0 auto' }}>
      <NotificationBanner />
    </div>
  );
};

export default NotificationBannerDemo;
